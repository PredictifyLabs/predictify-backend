# =============================================================================
# Predictify Backend - Variables de Entorno para Producción
# =============================================================================
# Copia este archivo a .env y configura los valores reales
# NUNCA subas el archivo .env a Git
# =============================================================================

# Database Configuration
POSTGRES_DB=predictify_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=TU_CONTRASEÑA_SEGURA_AQUI

# JWT Security - Genera una clave segura de al menos 256 bits (64 caracteres hex)
# Puedes generar una con: openssl rand -hex 32
JWT_SECRET=TU_CLAVE_JWT_SEGURA_DE_64_CARACTERES_MINIMO_AQUI
JWT_EXPIRATION=86400000

# Gemini AI (opcional)
GEMINI_API_KEY=

# CORS - Lista de orígenes permitidos separados por coma
ALLOWED_ORIGINS=https://tu-frontend.com,https://www.tu-frontend.com

# HikariCP Pool Size (ajustar según recursos del servidor)
HIKARI_MAX_POOL_SIZE=5
